[project]
name = "bpdf"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fire>=0.7.0",
    "fpdf2>=2.8.3",
]

[dependency-groups]
dev = [
    "ruff>=0.11.7",
]
